import React from 'react';

/**
 * 
 * @param {*} props 
 * This will display the header with the content as "TRUECALLER BLOGS"
 */

function Header(props) {
    return (
        <div id="header">
            <h2>TRUECALLER BLOGS</h2>
        </div>
    );
}

export default Header;